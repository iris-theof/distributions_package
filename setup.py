from setuptools import setup

setup(name='distributions',
      version='0.0',
      description='Bernoulli, Binomial, Gaussian distributions',
      packages=['distributions'],
      zip_safe=False)
